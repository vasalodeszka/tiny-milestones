name: Prod deployment

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    name: "Deployment"
    steps:
      - name: Check out
        uses: actions/checkout@f43a0e5ff2bd294095638e18286ca9a3d1956744 # v3
